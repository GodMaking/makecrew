---
name: git-workflow-and-versioning
description: Keep changes reviewable and reversible with small descriptive commits and safe branch synchronization.
---

# Git Workflow And Versioning

Inspect status before editing, keep one logical change per commit, run tests
before committing, and check staged content for secrets. Use ordinary pushes
and preserve remote history; never force-push as a default. Keep generated
build output and credentials out of the repository.
