interface:
  display_name: "Task Intake"
  short_description: "按任务选择直达、澄清、发现、确认或协作路径"
  brand_color: "#1769E0"
  default_prompt: "Use $task-intake to choose the shortest reliable path: execute clear routine work directly; ask only blocking questions; discover methods only when needed; wait for confirmation only for plan-first or consequential actions."

policy:
  allow_implicit_invocation: true
